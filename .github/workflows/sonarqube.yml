on:
  push:
  # Allows re-running the scan without pushing a commit, e.g. after
  # rotating SONAR_TOKEN.
  workflow_dispatch:
name: sonarqube
jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    # At job level so the scan step's `if` can see it: a step cannot test an
    # environment variable it defines itself.
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      # Coverage has to exist before the scan runs, or SonarCloud reports 0%.
      # Live-server tests stay opt-in, so this is the unit-test coverage only.
      - name: Run tests
        run: go test -coverprofile=cover.out -covermode=atomic ./...
      - name: SonarCloud Scan
        # nosemgrep: generic.secrets.security.detected-sonarqube-docs-api-key
        # The hex string is this action's pinned commit SHA, not a key. Pinning
        # is what SonarCloud's githubactions:S7637 asks for.
        #
        # Skipped when SONAR_TOKEN is absent, which is the case on dependabot
        # pull requests: they run without repository secrets, and a scan that
        # cannot authenticate would fail the check for a reason unrelated to
        # the change.
        if: env.SONAR_TOKEN != ''
        uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f # v8
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
